server:
  port: 8080
  host: "0.0.0.0"

database:
  driver: "org.sqlite.JDBC"
  url: "jdbc:sqlite:data/chaptervault.db"
  maxPoolSize: 5

storage:
  basePath: "downloads"
  defaultFormat: "CBZ"

log:
  level: "INFO"
