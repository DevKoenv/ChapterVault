services:
  chaptervault:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./downloads:/app/downloads
      - ./config:/app/config
    environment:
      - JAVA_OPTS=-Xmx512m
    restart: unless-stopped
